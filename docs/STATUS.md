# Court Scheduler — Status Log

Build history, chronological. Update as work lands. Spec/stack/conventions live in [CLAUDE.md](../CLAUDE.md).

Shorthand: **mig NNNN** = `supabase/migrations/00NN_*.sql`. **SD RPC** = `security definer` Postgres function (bypasses RLS for a narrow cross-user read/write). **FR** = final whole-branch review (dispatched on the most capable model after task-level reviews pass). **TDD** = test written/shown failing before implementation.

## v1

- [x] Supabase project + schema migrated.
- [x] Auth wired up.
- [x] Admin: set court availability.
- [x] Player: view open slots.
- [x] Player: book a slot.
- [x] Deployed to Vercel — live at https://court-scheduler-gold.vercel.app. Bug: Vercel project was connected before app code existed → Framework Preset stuck on "Other," silently served static output (404 on every route despite green builds). Fix: manually set Framework Preset → Next.js.
- [x] Concurrency verified: two simultaneous overlapping inserts same court hit DB directly, one `201` one `23P01` both times — exclusion constraint holds independent of app logic.
- [x] Player: `/bookings` page (view own bookings).
- [x] Start times offered every 15 min rolling (bookings stay 60 min). `computeOpenSlots` takes `durationMinutes`(60)/`stepMinutes`(15).
- [x] Time-selection UI v1: collapsible per-hour `<details>` sections. **Superseded.**
- [x] Time-selection UI v2: flat grid of on-hour blocks; new `TimeBlockPicker` (first `"use client"` component) — click extends selection to adjacent blocks, click across a gap resets to that block. Merged range → existing `/book` confirm page unchanged.
- [x] Date display cleanup: dates read "Wednesday, 8/26/2026" everywhere. New `src/lib/dateFormat.ts`: `formatBookingDate` (tz-aware instants), `formatCalendarDate` (noon-UTC trick, no date-boundary shift).
- 3 seeded courts set 9am–11pm daily via uncommitted one-off script, for picker testing.
- Bug fixed: `locations`/`courts`/`availability_rules`/`slot_overrides` RLS required `authenticated`, hiding courts from anon visitors. Fixed, mig 0003 (public read for non-sensitive facility fields).

## v2 — multi-location/multi-org

- [x] Player directory: `/` lists all locations w/ an active court across orgs → `/locations/[id]` → `/locations/[id]/courts/[id]` (previously hardcoded to one seeded court).
- [x] Admin manages multiple locations/courts: `/admin` = org dashboard (list/add locations); `/admin/locations/[id]` lists/adds courts, toggles `is_active`; `/admin/locations/[id]/courts/[id]` = per-court availability editor. New actions `createLocation`/`createCourt`/`updateCourtActive`. Auth via existing RLS only, no app-level checks.
- No schema/RLS change needed for the hierarchy itself (confirms v1 design). Gap found+fixed: `organizations` was member-only readable, blanking org name in player UI once embedded via `locations` — mig 0004 (public org read).
- [x] Mig 0004 applied live; full v2 admin CRUD flow (add location→court→availability→toggle) manually verified.
- [x] Court notes: `courts.notes` (mig 0005), editable per-court, shown to players when set.
- [x] Booking configuration requests (volleyball-specific): net height + court lines, optional. Booking now 2-step (slot grid → `/book` confirm page → `createBooking`). Stored on `bookings` (mig 0006, nullable, no RLS change). Shown on `/bookings` and admin's "Upcoming bookings" — no player identity shown (deliberate; no RLS path to player identity was asked for). Shared option lists in `src/lib/courtConfig.ts`.
- [x] Location timezone: real `<select>` of all IANA zones (`Intl.supportedValuesOf`) instead of free text.
- [x] Mig 0006 applied live; booking config verified end-to-end.
- Deferred by user: additional-organizations support (self-serve + admin-assisted club creation w/ ownership transfer) — full spec in `v2-org-creation-deferred` memory. [Later shipped, see "Organization creation" below.]
- Deliberate scope cuts, not gaps: org onboarding still manual/SQL; admin in multiple orgs lands on first membership.
- [x] Booking cancellation: `cancelBooking` (`src/app/actions/bookings.ts`) sets `status='cancelled'` — no schema change (exclusion constraint already `where confirmed`); RLS `bookings update own or member` decides who may cancel, action does no ownership check itself. "Cancel" button on player `/bookings` + admin court view.
- [x] Admin booking modification: `updateBookingConfig` edits requested net height/lines from admin court page. Still no admin visibility into *who* booked (deliberate).
- Note (later resolved): typecheck clean but full signup→book→cancel click-through not manually verified — sandboxed browser can't follow Supabase's email-confirmation link.
- [x] Per-court booking block size: owner picks "Full hour"/"Half hour" when adding a court; not editable after creation yet. New `courts.slot_size_minutes` (mig 0007, smallint default 60, check in (30,60), no backfill). Player page calls `computeOpenSlots` with both params = the court's setting → `TimeBlockPicker` auto-renders 30-min blocks, no picker changes.
- [x] Mig 0007 applied manually by user via Supabase SQL editor (no DB connection string/`supabase login` available then). Verified: half-hour test court renders 9:00/9:30/10:00…; test court then deactivated (no delete-court UI exists).
- [ ] ~~Location admin page to manage sites/hours~~ **Superseded below.**
- [x] Location admin hours page shipped at `/admin/locations/[id]/hours` + per-court date overrides. (1) "General Hours" form is a template, not stored — GET to `.../hours/confirm` lists every court that will be overwritten; "Apply to All Courts" (`pushHoursToAllCourts`) does the same delete-then-insert full-week replace into each court's `availability_rules` via a new shared `replaceAvailabilityRules` helper (kept in sync w/ the per-court editor); "Cancel" does no write. (2) `slot_overrides` (previously unused) gets a "Date Overrides" section: add/edit + upcoming-list w/ Remove, via `saveSlotOverride`/`deleteSlotOverride`. Upserts on `(court_id,date)` — mig 0009 adds that unique constraint. Validation (`validateSlotOverride`, `src/lib/slotOverride.ts`, TDD) — closed XOR both custom times, open<close. No role-gating (any org member reaches `/hours`; deferred to Club admins below). Verified end-to-end incl. pushing hours, closed-day override, validation-error case, removal, then restoring original hours.
- [x] Role-based nav sidebar: new `AppShell` client component from root layout replaces every ad-hoc header. Docked desktop / hamburger drawer mobile. Nav differs by signed-in + org-member status, not by `org_members.role` (role still ungated anywhere).
- [x] Post-login landing per user type: org members → `/admin` after sign-in; explicit `next` still wins. Change in `signIn`.
- [x] Email confirmations for booking/cancel: `createBooking`/`cancelBooking` send via Resend, incl. admin-cancels-on-player's-behalf. New `src/lib/email.ts` (2 pure TDD builders + thin `sendEmail` wrapper); send failure never blocks the booking action; no `RESEND_API_KEY` → warns + no-ops (verified). Cancel needs *owner's* email even when caller is admin, blocked by `users`' own-row RLS — fixed w/ SD RPC `get_booking_notification_email` (mig 0026), gated identically to the cancel RLS policy. Uses Resend's shared test domain for now (swappable via env var). Real delivery still unverified — needs a real `RESEND_API_KEY` in `.env.local`/Vercel (not done at time of writing).
- [ ] ~~Availability reconfiguration (per-slot blocking)~~ **Superseded below.**
- [x] Per-slot availability blocking shipped — recurring (day-of-week) + one-off (date), one `blocked_slots` table (mig 0014; `day_of_week` XOR `date` picks the mode). Granularity matches court's `slot_size_minutes`. `computeOpenSlots` gained `blockedSlots` param. New `generateSlotStarts` backs the admin grid-builder (`buildSlotGrid`, `src/lib/blockedSlots.ts`); `computeOpenSlots` doesn't reuse it (different loop-boundary key: step vs. duration, not always equal) — reimplements the walk directly to avoid silent misalignment. Admin UI: query-param mode/day/date picker + per-slot toggle grid. `blocked_slots` public-select from day one (lesson from the earlier `availability_rules` gap). Blocking never touches existing bookings (verified). Verified end-to-end: recurring block hits only that weekday; one-off hits only that date even under an active recurring block on a different weekday; both unblock cleanly. FR fixes: `?date=` validated format but not calendar validity (`2026-02-30` silently defeated blocking via discarded Postgres errors) — now round-tripped through `Date`. `toggleBlockedSlot` treats `23505` unique-violation (double-click/race) as a no-op, matching `createBooking`. Deferred, not fixed: `createBooking` still doesn't server-side recheck blocked/closed before insert (pre-existing, more consequential now); admin's date-mode grid only queries date-specific blocks, so a recurring block covering the viewed date shows as an ordinary togglable-open slot with no indication it can't actually be booked.
- [x] Save confirmation messages: every silent admin mutation now redirects w/ a query-param flag + small green confirmation ("Notes saved.", "Court activated."/"deactivated.", "Availability saved.", "Location added.", "Court added.", per-booking "Saved."). Cancellations show "Booking cancelled." too. New shared `SuccessBanner` for full-width; small keyed inline text for per-row ones (`notes_saved`, `active_changed`, `config_saved`). Redirect-borne query param, no JS toast — consistent w/ the app's existing `booked=1` pattern. `cancelBooking` gained `redirect_to` (called from 2 pages needing different landings).
- [x] Court details editable post-creation: "Edit court" `<details>` → one `updateCourt` action (replaces notes-only `updateCourtNotes`).
- [x] Location details (name/address/timezone) also editable post-creation, same pattern, `updateLocation`. Timezone edit uses `revalidatePath("/locations/[id]", "layout")` since it affects every court under it.
- [ ] ~~Dedicated booking confirmation page~~ **Superseded below.**
- [x] Dedicated confirmation page shipped at `/bookings/[id]`. `createBooking` redirects there instead of the old `?booked=1` banner (removed). Shows date/time/court/org/config/status + Cancel (reuses `isCancellable`). RLS alone gates visibility (inaccessible booking → 404, no app check). `/bookings` list links via "View details." Add-to-calendar + delivery-verified email remain future work.
- [ ] ~~Weather widget on court date view~~ **Superseded below.**
- [x] Weather widget shipped, Open-Meteo (no key needed) via `src/lib/weather.ts` (`fetchHourlyForecast`). Hourly card strip (emoji/temp/precip%) scoped to the court's *actual open hours* that date — required promoting the open/close resolution buried in `computeOpenSlots` to its own exported `resolveDayHours` (now also unit-tested, `availability.test.ts`, first tests that module ever had). Renders nothing (no error state) when: no coordinates yet, court closed that day, or date beyond Open-Meteo's ~16-day range — any fetch failure treated the same (fails silently). Scoped to every court regardless of indoor/outdoor (no new concept added), per user. Admin: `/admin/locations/[id]` shows a yellow "no coordinates" banner (previously buried in a collapsed disclosure). `filterHoursToWindow`/`resolveDayHours` TDD. Verified: real forecast matching 14 open hours for a verified location; widget disappearing when coords cleared (banner appears); disappearing again >1 month out (booking slots unaffected).
- [x] Title case for all headings/nav labels (dynamic data like location/court names left as-is).
- [x] Sidebar dark-mode polish: `AppShell` sidebar/header got `dark:` variants instead of flat `bg-white`. Fixed `w-64` width untouched (layout, not asked).
- [ ] ~~"My bookings" tabs~~ **Superseded below.**
- [x] "My bookings" tabs shipped + a related fix: an in-progress booking (started, not ended) is now its own time state, not lumped w/ upcoming. New `src/lib/bookingStatus.ts` (`categorizeBookingTime`/`groupBookingsByTime`/`isCancellable`) — first TDD module in the project, boundary cases (exactly at start/end) covered before implementation. `/bookings` gets Upcoming/Past tabs via `?tab=` (server-rendered, no client component); Upcoming shows in-progress first, labeled "In progress." Cancel gated on `isCancellable` (confirmed AND upcoming) — previously an in-progress/past-but-confirmed booking was cancellable, since nothing auto-transitions status by time. Admin's "Upcoming Bookings" list already excluded in-progress/past via its own filter — left as-is (switching would newly *show* in-progress bookings there, an unasked scope change).
- [x] Fixed reported bug: editing location/court showed no save confirmation — the confirmation text was rendering correctly all along, just inside the Edit `<details>` disclosure which resets closed on the post-save redirect. Both disclosures now stay `open` when keyed off their own save flag.
- [x] Location address validation/autofill + maps links: new `AddressLookup` client component hits new `/api/geocode` (proxies OSM/Nominatim, no key needed). Fills simplified address + `postal_code`/`latitude`/`longitude`/`formatted_address` (new nullable cols, mig 0008) — also unblocks the Weather widget's coordinate need. Player pages now link address to Apple/Google Maps (`src/lib/maps.ts`) by platform, precise coords when available. Existing locations show "not yet verified" until re-saved through lookup (no backfill, same pattern as `slot_size_minutes`).
- [x] Mig 0008 applied live; verified end-to-end for all 3 seeded locations. Also: direct Postgres access configured — `DATABASE_URL` (Session pooler; direct host is IPv6-only, doesn't resolve here) + new `npm run migrate -- <file>` (`scripts/run-sql.mjs`, `pg` package). Future migrations no longer need manual SQL-editor round-trips.
- [x] Searchable timezone picker replacing the raw 400-entry `<select>`: new `TimezoneSelect` combobox, "City, State, Country (GMT±H:MM)" results (`src/lib/timezones.ts`, `countries-and-timezones` + live `Intl` offsets), sorted by city, current pinned top, matches aliases too (e.g. "Pacific Time"→`America/Los_Angeles`). Fully custom-styled incl. `dark:` (native `<select>` popups can't be restyled — the actual bug behind the "unreadable in dark mode" report). Geocoded address auto-fills timezone via `tz-lookup` (server-side in `/api/geocode`), still manually overridable. New `LocationFormFields` composes both + shared state.
- [x] Fixed same dark-mode class of bug elsewhere: light-only `hover:bg-gray-50/100` w/ no `dark:` variant on 4 more surfaces (Find a Court cards, admin locations list, location's court list, `TimeBlockPicker` hour buttons) — added `dark:border-neutral-800 dark:hover:bg-neutral-800`.
- [x] Fixed reported bug: no way to shrink a multi-block `TimeBlockPicker` selection (re-clicking an edge just re-extended it). Now: clicking start/end edge of a multi-block selection removes that one block; clicking an interior block resets to just that block (can't shrink w/o breaking contiguity), matching the existing "gap resets" convention.
- [x] Test infra added: vitest + RTL, jsdom (`vitest.config.mts`/`vitest.setup.ts`). `npm test` (once) / `npm run test:watch`. First suite: `src/lib/maps.test.ts`. TDD convention adopted going forward.
- [ ] ~~Club admins created in-app~~ **Superseded below.**
- [x] Club admins shipped: owner/admin grants an existing player admin access by email at `/admin/team` (owner/admin only), role `admin` or `staff` (`owner` assignment out of scope, deferred to org-creation spec). 3 new actions + SD RPC `lookup_user_id_by_email` (mig 0010, since `users`' RLS only allows selecting own row). `role` gates something for the first time: `locations`/`courts` writes tightened to owner/admin only; `availability_rules`/`slot_overrides`/`bookings` stay open to staff. Admin location page hides staff-restricted controls entirely (doesn't rely on RLS rejection). `wouldRemoveLastOwner` guard (`src/lib/orgRoles.ts`, TDD) blocks removing/demoting an org's last owner. Membership lookup (dup'd 3x) consolidated into shared `getCurrentMembership`/`getRoleForOrg`. Mig 0011 adds `list_org_member_emails` (2nd SD RPC, org-member-scoped) for the `/admin/team` roster. Mig 0012 tightens `lookup_user_id_by_email` to owner/admin-only callers, closing an FR-found account-existence-oracle gap (any authenticated user could call it). Verified live: added real 2nd account as staff, error states, role change/revert, access removal, last-owner-removal blocked, staff sees cut-down location page while hours/overrides/bookings unaffected — though the staff-UI check itself was via code review not live sign-in (only browser session was the real owner, no on-file password to recover if lost). Left deliberately open at the time: any admin (not just owner) could demote/remove an *owner* — `wouldRemoveLastOwner` only protects the *last* owner. **[Closed later — see "Org owner-row protection" below.]**
- [ ] City-scoped home page w/ default city at signup **[Superseded below — partially, then fully.]** Wanted: constrained-list city picker at signup, `/` defaults to that city, session-override switcher. Blockers at the time: `locations` had no city/metro column; no app-level `users` profile row existed at all (`signUp` only creates the Supabase Auth user).
- [x] Superseded (partially): City→Club→Location→Court drilldown shipped. `/` shows a city list → `/cities/[city]` → `/clubs/[orgId]` (new — shows *all* of a club's locations, since one club can span cities) → `/locations/[id]`. New nullable `locations.city` (mig 0013, no backfill), populated via the geocode flow (`extractCity` pulls Nominatim's already-computed city, previously discarded into free-text address). Two TDD grouping fns (`src/lib/cityGrouping.ts`): `groupLocationsByCity`/`clubsInCity`. A location with no city shows under "Other locations" on `/`. Personalization half (default city at signup, session override) still deferred.
- [x] UI consistency pass 1/2: (1) native `<select>` dropdown popups always rendered in light-mode chrome regardless of theme — fixed w/ one `color-scheme: dark;` in the existing dark-mode media block (confirmed via computed style). (2) every light-only status/info banner (no `dark:` variant) across ~7 surfaces gained `dark:bg-*-950 dark:text-*-300`. CSS-only, no new tests. 3 findings deferred to part 2 below.
- [x] UI consistency pass 2/2 (the 3 deferred findings): (1) every admin page's heading was `<h2>` w/ no page-level `<h1>` anywhere under `/admin` — promoted to `<h1>` on all 6 admin pages (subsection headings stay `<h2>`). (2) login/signup heading didn't match the responsive `text-xl sm:text-2xl` convention — matched. (3) Blocked Slots day picker's selected-day `border-black` was invisible in dark mode — added `dark:border-white`.

## v3 — Special Events (current)

- [x] Special Events core (Plan 1/4) shipped — locations host tournaments/leagues/open-play/clinics as `events`/`event_sessions`, browsable through the City→Club→Location hierarchy. No registration/teams/brackets yet. Spec `docs/superpowers/specs/2026-08-30-special-events-design.md`, plan `-core.md`. **Court-blocking architecture:** each `event_sessions` row also inserts into the existing `bookings` table (new `source` col `'player'|'event'`; nullable `event_session_id` FK cascade; `user_id` now nullable) — so an event's court time is governed by the *same* GIST exclusion constraint (mig 0015), zero changes needed to `computeOpenSlots`/`booked_slots`. Admin: `/admin/locations/[id]/events` list/create, `/admin/locations/[id]/events/[eventId]` manage sessions. Player: `/events` (city-grouped, soonest-first, `src/lib/eventGrouping.ts` TDD) + new sections on city/location pages → `/events/[eventId]` detail. Draft/cancelled events excluded from all player-facing lists (direct link still shows cancelled, w/ notice). Built via subagent-driven-development, 7 tasks + Task-1 fix (missing insert RLS policy for event-sourced bookings, mig 0016) + one FR fix round: event-sourced `bookings` rows were rendering as ordinary cancellable player bookings on the admin court page (cancelling one would've freed a live event's court time) — fixed by rendering `source='event'` rows read-only plus a defense-in-depth guard directly in `cancelBooking`. Same FR pass also fixed: missing `revalidatePath` after session add/remove; admin event-detail page not scoped to its own location (wrong-location timezone derivation possible); dead "event created" banner; cancelled events still showing as upcoming on player lists. `fee_cents`/teams/brackets deliberately absent (Plans 2–4). Build order updated: Special Events now precedes Payments.
- Expanded seed data (uncommitted, service-role key): every location now 3–8 active courts, full weekly hours. 3 more tournaments added (varied status/mode) alongside Fall Open Tournament. 2 test accounts created via Admin API (`email_confirm:true`): **`test.player@courtscheduler.dev`** / **`test.owner@courtscheduler.dev`** (owns Ace Volleyball Club), both password **`TestPass123!`**.
- [x] Special Events registration (Plan 2/4) shipped — individual sign-up, self-formed team (captain + name-only roster), admin-assembled team, capacity w/ auto waitlist/promotion, "My Events." Plan `-registration.md`. 3 new tables (`event_teams`/`event_team_members`/`event_registrations`) + 2 SD RPCs: `promote_next_waitlisted` (FIFO), `list_event_registrant_emails`. New `event_registration_counts` view (privacy-preserving, mirrors `booked_slots`), since `event_registrations` itself isn't public-select. Capacity is app-level recount-then-insert (soft business limit, not a DB constraint like court-booking). `registered_at` forced server-side via `before insert` trigger (mig 0018) — a client-supplied timestamp would otherwise let a player jump the waitlist queue. 6 tasks, 5 needed a fix round each.
  - **FR found 2 Critical composition bugs:** (1) `registerForEvent` counted capacity/waitlist under the *caller's own* RLS visibility (zero rows for a plain player) instead of the `event_registration_counts` view — capacity was never enforced, waitlist didn't function. (2) "am I registered" was derived from `event_teams.captain_user_id` instead of `event_registrations` — broke 3 ways: permanent lockout after cancelling a self-formed team (team row survives forever); waitlisted team shown as registered; duplicate registration possible post-assembly. Fixed by making `event_registrations` the sole source of truth everywhere. 5 further Important fixes same pass: no DELETE policy on `event_teams`/`event_team_members` (silent rollback no-op, directly caused half of bug 2); `event_registrations` update policy had no `with check` (self-PATCH `waitlisted`→`registered` queue-jump, same class the `registered_at` trigger closed on insert); `assembleEventTeam` never called `promote_next_waitlisted` and hardcoded new team status `'registered'` regardless of fullness; `registerForEvent` had no server-side event-status gate (draft/cancelled/completed events registerable by direct POST) — fixed w/ minimum-viable `status in ('published','registration_open')` check (timing-window validation still deferred); admin-assembled teams (no captain) were uncancellable by anyone.
  - 2 lessons: the `assembleEventTeam` status fix initially double-subtracted counts (read `event_registration_counts` *after* deleting originals, then subtracted again) — implementer caught it via live repro before sign-off. Separately, the uncancellable-admin-team fix landed RLS-only, missing the matching UI-gate update on `/events/registrations` — caught by a re-review, applied+verified after a session interruption. `fee_cents`/brackets remain deliberately absent.
- [x] Special Events brackets (Plan 3/4) shipped — single-elim, double-elim, round robin, pool play; per-set results; auto-advance; standings; player-visible. Spec/plan `-brackets-design.md`/`-brackets.md`. New `event_matches` (generic bracket/round/slot shape, self-referencing `*_advances_from_match_id`) + `event_match_sets` + nullable `event_registrations.display_name` (mig 0020). 4 pure generation fns (`src/lib/bracketGeneration.ts`); pure `propagateAdvancement` (flags rather than silently overwrites an already-completed downstream match); pure `computeStandings` (win%→point diff→head-to-head). **Mid-design fix:** matches originally had no time info (useless for a weekly league) — added optional `event_matches.session_id` linking to an `event_sessions` row, settable manually or via bulk "Auto-assign to sessions" (`pairMatchesToSessions`, simple ordered zip, no capacity-awareness). Admin gets manual overrides: edit any match regardless of status; withdrawing a registration forfeits or substitutes.
  - **4 bugs found only by manual browser verification** (tests missed all): (1) **Critical** — double-elim losers-bracket round 1 advanced the *winner* not the *loser* of each WR1 match, inverting double-elim semantics at its first step; no test checked LR1's `advancement_type` — new regression test added. (2) admin "Edit Match" `<select>`s used uncontrolled `defaultValue` (only applies on first mount; Next.js Server Action redirects are client-side transitions) — saving any field on an already-decided match silently blanked the winner; fixed by remounting the form keyed on mutable fields. (3) a bye auto-completes at generation time, but both the Regenerate button and its guard checked for *any* completed match — permanently hid Regenerate for any bracket with a bye; both now exclude `is_bye`. (4) round robin/pool play player view only showed standings, not the flat match list w/ session/court info the spec called for — added. Minor: withdrawn-player name lookup excluded cancelled registrations, showing "TBD" for historical byes/matches — split into a separate unfiltered query.
  - Verified live: 6-player single-elim w/ auto byes/forfeit/cascade-warning/withdrawal (forfeit+substitute paths); corrected double-elim losing-bracket routing; 4-team round robin w/ weekly sessions + auto-assign scheduling; mobile horizontally-scrolling bracket tree. Pool play and a full double-elim-to-grand-final playthrough not live-verified beyond the unit suite (reasonable stopping point given coverage). Follow-up backlog, not part of this plan: capacity-aware auto-scheduling; user profiles (name/gender/level) as a cross-cutting feature. Payments (Plan 4/4) is the only piece left before v3 completes.
- [x] Team roster visibility & membership integrity shipped — any player sees a self-formed team's full roster; every roster spot resolves to a real account (immediately, or via a pending invite claimed at first sign-in). Spec/plan `-team-roster-visibility-*.md`. New nullable `event_team_members.invited_email` (unique while pending, mig 0021) + 2 SD RPCs: `find_registered_user_by_email` (exact-match only, no list, not a new enumeration surface) and `claim_pending_team_invites` (reads only `auth.uid()`/`auth.users`, no params). `registerForEvent`'s self-formed path now requires name+email per slot; email lookup happens before insert. **Design correction found pre-code:** spec called for claiming invites in `signUp`, but `supabase.auth.signUp()` has no authenticated session in this app (email confirmation required first) — moved the claim call to `signIn` instead. `invited_email` is never selected/rendered player-facing (shows plain "(pending)"); this also fixed a pre-existing leak where a captain's own roster row defaulted its `display_name` to their raw email. Verified live end-to-end: matching-email teammate linked immediately; non-matching got "(pending)"; inviting an already-pending email elsewhere produced a clean-rollback collision error; name-without-email rejected pre-write; signing in as a brand-new account with the pending email auto-linked the spot and correctly recognized existing registration status. Pre-existing free-text-only rosters (predating this migration) render as "(pending)" without erroring — accepted imprecision, no backfill. Self-removal for a non-captain teammate and real player-name search remain out of scope.
- [x] User profiles shipped — wires up `users.name` (existed unused since `0001_init.sql`) + new `gender`/`skill_level`, a `/profile` page, and a gate requiring a complete profile to register for anything but `open_play`. Spec/plan `-user-profiles-*.md`. 5 tasks, 1 task-level fix (open redirect via unvalidated `next` param, fixed in-scope). **Skill level:** two-path picker (`SkillLevelPicker.tsx`) — letter ratings (Rec/B/BB/A/AA/Open) or a coarser plain-language scale mapping onto the same letters; only the letter value is ever stored (hidden input). **Same signUp/signIn timing issue as the roster-invite plan:** spec wanted signup to collect these fields, but that request has no authenticated session either — no safe deferred moment existed this time (unlike roster-invites' signIn hook) without inventing a new unauthenticated write path, so signup stayed email/password-only and `/profile` became the sole place to set a profile.
  - **FR found 4 Important composition bugs:** (1) `/profile` never said *which* field was missing on a failed save — added a "Still missing: ..." list. (2) `updateProfile`'s `?message=` redirect target never read/rendered the param — silently dropped. (3) **Most consequential:** the event registration form rendered regardless of profile completeness, so a player could fill a full team + up to 5 teammates and lose it all to the gate's redirect with no warning — fixed by gating the form's *render* (not just the server action) on `isProfileComplete`. (4) The Task-3 open-redirect fix (`isSafeRedirectPath`) was duplicated, untested, and left an identical unvalidated `next` in `signIn` (higher-traffic than `/profile`) — extracted to shared, tested `src/lib/redirects.ts`, `signIn` fixed too even though outside the original 5 tasks. 3 Minor fixes bundled: server-side allowlist validation for `gender`/`skill_level` (previously only the `<select>` UI constrained them); zero-rows-affected check on the update (established pattern); fixed `${next}?message=...` blindly appending `?` without checking for an existing query string.
  - Deliberately parked: `SkillLevelPicker` no "currently saved: X" hint when a letter-only value has no plain-language mapping (**later fixed, see below**); rostered/invited teammates never individually pass the profile gate (only the captain does) — known boundary, not fixed; unvalidated `message` param on `/profile` (consistent w/ existing pattern elsewhere).
- [x] Superseded: `public.users.email`/`role` self-editing closed via a `before update on users` trigger (mig 0023) rejecting any self-service change to either column (RLS's `users update own` is row-level only, can't express column-level protection). `name`/`gender`/`skill_level` stay freely editable. Root-cause fix: every RPC trusting `users.email` (`find_registered_user_by_email`, `lookup_user_id_by_email`, `list_org_member_emails`, `list_event_registrant_emails`) becomes safe without touching any of them. Same trigger also closes a related latent issue: nothing authorizes off `users.role` today, but a player could otherwise have self-promoted to `'org_admin'`. Verified: legit `name`/`gender`/`skill_level` update still works; `email`/`role` self-edit rejected, row confirmed unchanged.
- [x] Superseded: public-facing player profiles shipped. Spec/plan `-public-player-profiles-*.md`. Opt-in (`share_stats_publicly` checkbox, off by default) → `/players/[userId]` (name/skill/all-time W-L-games), reachable only by link (never a browsable directory). New `users.share_stats_publicly` + SD RPC `get_public_player_stats(p_user_id)` (mig 0024) computes the tally live, no cache/backfill — returns empty identically whether nonexistent/opted-out/malformed id (no existence-probing), never returns gender. 5 tasks, all reviewed clean individually, + 1 FR fix wave.
  - **FR caught a Critical bug no task review could:** the page deciding which names get linked (`events/[eventId]/page.tsx`) queried `users` directly filtered on `share_stats_publicly=true` — copied verbatim from the spec, which hadn't accounted for the existing `"users select own"` RLS. That policy meant a player's session could only ever see *their own* row via a plain select — every roster/bracket link silently rendered as plain text in production, 2 whole tasks fully inert, despite 122 passing tests and clean task reviews (Task 1's own DB verification ran as Postgres superuser via `DATABASE_URL`, which bypasses RLS entirely — never exercised the actually-broken path). **Trap:** testing signed in as the same account that opted in (looking at your own name on a roster) works fine — that's the one case `"users select own"` still allows; only a *second* account or a signed-out visitor exposes it. Fixed w/ SD RPC `filter_public_profile_user_ids(p_user_ids uuid[])` (mig 0025) — deliberately not a broadened `users` RLS policy (would leak opted-in players' email/gender/phone to `anon`). Re-verified both via role-impersonation against the DB and a genuine 2-account browser pass. 2 smaller Important fixes rode along: `MatchCard`'s keyboard handler was swallowing a focused nested profile link's own Enter-to-navigate; opt-in lookup errors were silently discarded. Minor: the conditional-link-vs-plain-text rendering (dup'd 3 ways) consolidated into shared `PlayerNameLink`.
  - **Takeaway carried forward:** verifying via Postgres superuser (`DATABASE_URL`) proves the SQL is correct, not that the *app* can run it under RLS. The registration plan already learned this once; "the spec prescribed this exact query" is a reason to check harder, not to trust it.
- [x] Superseded (fully): city-scoped signup personalization shipped, completing the earlier city-scoped-home-page item. Spec/plan `-city-scoped-signup-personalization-*.md`. New nullable/defaulted `users.default_city`/`city_prompt_dismissed` (mig 0027). "Set at signup" became "set at first login" (same `signUp`-has-no-session constraint as before): `signIn` redirects a non-org-member player w/ no default city (and not-yet-dismissed) to a one-time `/choose-city` (skippable, permanently); editable anytime after via `/profile`. `/` is now conditional via pure `resolveHomeCity` precedence (override cookie → stored default → null = show everything, TDD). Both render paths extracted to shared components (`CityContent`, `AllCitiesContent`) used by every caller, moved the full-directory view to new `/cities`. Session override = plain browser cookie (`city_override`, no `maxAge`, set only via explicit `setCityOverride` action), works signed-out too.
  - 9 code tasks, 1 fix round (Task 4: `setDefaultCity`/`skipCityPrompt` silently treated a failed/zero-row update as success — same bug class already fixed once in `updateProfile`, fixed the same way). Verified end-to-end w/ 2 fresh Admin-API test accounts: first-login prompt + permanent skip; prompt + set city (shows directly next login); session override signed-in (differs from stored default) and signed-out (no stored default, "Clear" only); `/profile` edit propagating to `/` immediately (`revalidatePath("/")`); stale-city fallback (temporarily deactivated every court in a city → graceful full-list fallback, then reactivated). Deferred Minor findings: sequential (non-`Promise.all`'d) queries on `/` (**later fixed below**); `listActiveCities`+`isKnownCity` validation dup'd 3 ways (consistent w/ existing no-shared-validator convention); missing WHY-comment on new ordering-dependent block in `signIn`.
- [x] Superseded (fully): "Add to calendar" links shipped, closing the booking-confirmation-page backlog item. Google/Outlook web links + hand-rolled RFC 5545 `.ics` (`src/lib/calendarLinks.ts`, no new dependency — short enough for a pure function), served by new `GET /api/bookings/[id]/ics`, which defers to the same RLS as `/bookings/[id]` (no own ownership check). All 3 links on `/bookings/[id]` + in the confirmation email (`BookingEmailDetails` gained raw `start_time`/`end_time`). `getAppUrl()` moved from `actions/bookings.ts` to shared `src/lib/appUrl.ts`. Verified live w/ a fresh Admin-API test account: booked slot, confirmed all 3 links + correct UTC encoding, fetched `.ics` directly (`Content-Type: text/calendar`, `Content-Disposition: attachment`, well-formed `VEVENT`). Cancellation emails intentionally get no calendar links.
- [x] Fixed `SkillLevelPicker` UX rough edge deferred above: switching to the plain-language picker while the saved value is a letter with no plain-language mapping (`AA`, `Open`) reset the visible `<select>` to "-- select --" though the hidden value was never actually lost — now shows "Currently saved: {value}." First RTL component test in the project (`SkillLevelPicker.test.tsx`), 3 cases. Suite (153 tests) + `tsc` clean.
- [x] Parallelized the sequential `/` queries deferred above: `src/app/page.tsx`'s `auth.getUser()` + `listActiveCities` (independent) now `Promise.all`'d; `CityContent.tsx`'s `locations` query + independent `events` query likewise. No behavior change, no new tests (matches this codebase's reordering-only convention) — verified via suite (153, unchanged) + live browser pass (signed-out all-cities view + `/cities/New York` identical to before).
- [x] Closed (partially) the teammate-profile-check gap flagged above: `registerForEvent` now also checks profile completeness for any teammate email resolving to an *existing* account (via `find_registered_user_by_email`), rejecting w/ a friendly per-teammate error + the existing orphaned-team rollback path. A pending invite (no account yet) still can't be checked — real, documented limitation, not silently dropped. New SD RPC `is_profile_complete_for_user(p_user_id)` (mig 0028, applied live) returns a bare boolean (same opaque-result pattern as `find_registered_user_by_email`). No new unit test (server actions verified live per convention). Verified w/ 2 fresh Admin-API accounts: incomplete-profile teammate rejected w/ no orphaned team row; completing the profile then succeeding w/ real `user_id` links and `status='registered'`. Test data removed after.
- [x] Closed the last item deferred from Club Admins: non-owner admins could demote/remove an *owner*. Two layers: (1) app — pure `canActOnMember(actorRole,targetRole)` (`src/lib/orgRoles.ts`, TDD) blocks a non-owner acting on an owner target, + zero-rows-affected checks on both actions (previously an RLS-blocked update matched zero rows w/ no error, sailing past to a false success banner). (2) DB — `org_members` insert/update/delete policies (mig 0029) now require `role <> 'owner' or is_org_owner(org_id)` (new helper, same pattern as `is_org_member`/`is_org_admin`) — this also closed a more severe gap found while writing the migration: nothing previously stopped a non-owner admin from directly PATCHing `role='owner'` on *any* row via raw API (self-promotion), even though the app itself never exposed this path. `/admin/team`'s Remove button now also hidden for owner rows when the viewer isn't an owner. Verified live w/ a real test admin: UI hid Remove on owner rows; a real-session (non-superuser) PostgREST PATCH/DELETE against an owner row matched zero rows; a self-promotion PATCH attempt got a hard `42501`. Process note: an earlier verification attempt tried `set local role authenticated` inside a raw `psql`/`DATABASE_URL` session, which silently does nothing (that connection is a superuser/table-owner role, bypasses RLS regardless) — briefly mutated then deleted the real `test.owner@courtscheduler.dev` account's membership for real before being caught and reverted; the actual verification was redone correctly through the real app session + PostgREST.
- [ ] ~~Profile photos~~ / ~~Optional social media links~~ **Superseded below (shipped together).**
- [x] Manual Venmo payments for event registration shipped — lets an org charge a flat fee and collect it via Venmo, "pending→paid/refunded" tracked in-app. Deliberately **not** real payment processing (Venmo has no public confirmation API) — a stopgap for small/private events, distinct from the still-not-started v4 Stripe Connect phase. Spec/plan `-venmo-manual-payments-*.md`. New nullable `organizations.venmo_handle`/`events.fee_cents` (mig 0030, no backfill) — `event_registrations.payment_status` (existed unused since the registration plan) gets its first reader/writer. `registerForEvent` sets it via pure `initialPaymentStatus` (`src/lib/eventRegistration.ts`); registrant sees a "Payment due" panel w/ a working Venmo deep link (`buildVenmoPaymentUrl`, `src/lib/venmoLink.ts` — unofficial-but-stable convention, handle+amount text always shown alongside) on the event page + "My Events." Org member marks paid (sends confirmation email via `buildPaymentConfirmationEmail` + SD RPC `get_registration_notification_email`, same pattern as booking emails) or refunded (no email) from a new "Registrants & Payments" admin section. `createEvent`/`updateEvent` reject a nonzero fee when the org has no Venmo handle, server-side. 8 tasks, all reviewed clean individually.
  - **FR found 4 Important composition bugs:** (1) a *waitlisted* registration on a paid event still got `pending` status + a "pay now" panel for a spot the player doesn't have — fixed by gating the panel on `status==='registered'` (no write-path change needed; later promotion just works). (2) `updateOrganization` guarded against *setting* a fee w/ no handle but not against *clearing* the handle while paid events already existed — fixed by checking every location under the org first. (3) admin's Registrants & Payments list filtered out *all* cancelled registrations, hiding the primary real-world refund case (a paid registration that gets cancelled) exactly when "Mark Refunded" was needed — fixed w/ an `.or()` filter (keep non-cancelled + any cancelled-but-paid/refunded row) + "(cancelled)"/"(waitlisted)" labels. (4) the Venmo note used the player's *profile* name for individual registrations while admin roster + email both used the registration's own `display_name` — defeated the note's whole payment-matching purpose — unified on `display_name`. 2 Minor bundled: `updateEvent` checked one org's handle but didn't scope the actual update to that org's location (a handle-less org's member could paste in a different org's location id to bypass the guard) — fixed w/ `.eq("location_id", locationId)`; entering "0" stored `fee_cents:0` instead of `null` (breaking the "blank=free" convention) — fixed. One scoped re-review confirmed all 6 fixes, no new breakage.
  - Verified genuinely live in the browser (worked around a worktree-vs-dev-server tooling mismatch by pointing the Browser pane at a bare `localhost` URL instead of the name-based lookup): real Venmo handle, real $15 event, fresh test account, payment-due panel + correct link both sides, marked paid (email path reached, gracefully no-op'd w/ no `RESEND_API_KEY`), marked refunded (no email). Real-session RLS check confirmed a non-org-member cannot write another player's `payment_status`.
- [ ] "Blind Draw" random team generation — one click to auto-split individually-registered players into balanced teams (vs. today's all-manual `assembleEventTeam`). Needs a skill-rating column (schema has none today — scale/source TBD) + a balancing algorithm. Open questions: where rating is set (self-reported/admin-assigned/both), fixed vs. as-even-as-possible team size, all-registrants-vs-subset, pre-commit editability.
- [x] 5 bracket/scoring UX fixes shipped from live testing feedback: (1) short format-description blurbs under the bracket-generation Format picker. (2) Per-event match scoring config — new `events.best_of_sets`/`points_per_set`/`win_by` (mig 0031, defaults best-of-3/first-to-21/win-by-2, no backfill). `recordMatchResult` validates every set via pure `isValidSetScore` (TDD) — winner must reach `points_per_set` w/ margin ≥ `win_by`, no hard cap (deuce sets can run past target); bad scores rejected w/ a specific "Set N: not a valid score" error. Enter-Result form now renders exactly `best_of_sets` rows instead of a hardcoded 5. (3) Already-saved scores now editable — "Edit Result" re-opens pre-filled, resubmitting through the same `recordMatchResult` (already delete-reinsert + recompute) just works; reuses the existing correction-cascade banner. (4) Team/player names now shown as column headers above Set N inputs. (5) new always-visible "Registrants" section on the admin event page (team rosters or flat individual list), distinct from the fee-gated "Registrants & Payments" section. Verified live on the seeded tournament: generated round robin, edited a completed match's score and confirmed pre-fill + updated standings, confirmed an invalid win-by-1 rejected and left untouched. Tests (172) + `tsc` clean. Deliberately not addressed (noted for separate specs): a visual/interactive bracket, free-agent registration for team events.
- [x] Interactive visual bracket shipped — replaces the flat match list w/ a real connector-line tree (`bracketry` npm library) for single/double-elim, a tappable card grid for round robin/pool play, and a mobile-first bottom sheet for scoring, w/ round-based locking (only the earliest fully-completed-before-it round is scorable). Brainstormed w/ user (bottom sheet over full-screen/inline-expand; horizontal-scroll-with-peek over one-round-paging). Spec/plan `-interactive-bracket-*.md`. 10 tasks all reviewed clean + 1 extra fix cycle.
  - Architecture: pure `toBracketryData` (converts this project's match/set rows to bracketry's shape); pure `computeActiveRounds`/`isMatchTappable` (deliberately stricter than the schema requires — holds back a whole round even if one match in it already has both teams known, since advancement propagates per-match). New `src/components/bracket/` tree: `InteractiveBracket` orchestrates, routes elim groups to `BracketryTreeView` (one bracketry instance per group) and round robin/pool to `MatchCardGrid`; both open a shared `MatchResultSheet`. `recordMatchResult` changed from redirect-always to `{ok,error}`/`{ok,reviewNeeded}` via React 19 `useActionState` (inline errors, no lost scroll position). Player-facing page gets the same treatment read-only; old `MatchCard.tsx` deleted.
  - **Real bug found only by live verification** (not typecheck/RTL, which use jsdom w/ `document` always present): `bracketry`'s bundle calls `document.createElement()` unguarded at module top level, crashing Next.js's server render of the importing `"use client"` component even though `createBracket()` itself never runs server-side — reproduced by importing the raw bundle in plain Node. Fixed via `next/dynamic({ssr:false})`; that fix then broke 3 (+1 vacuously-passing) of 5 `InteractiveBracket` tests (async dynamic import resolves too late for a synchronous post-`render()` callback invocation under jsdom) — repaired w/ `await waitFor(...)` guards, each independently reviewed.
  - Verified live end-to-end: single-elim scored through advancement, confirming round-2 stays locked while a sibling round-1 match is pending and unlocks the instant it finishes; invalid win-by-1 rejected inline w/ sheet staying open/intact; double-elim rendering 3 independent bracketry instances (winners/losers/playoff) w/ no cross-instance interference; player read-only sheet; mobile bottom sheet + horizontal-scroll-with-peek. Tests (192) + `tsc` clean. Deliberately deferred per spec: player-profile deep-links, per-match session/court time in the tree/grid/sheet.
- [x] Theme system Phase 1 shipped — real design-token foundation (2 named palettes, since replaced) + manual light/dark toggle (sidebar since replaced by top nav — **see Open Court retheme below**). Brainstormed via inline mockups (3 directions compared). Spec/plan `-theme-system-phase1-*.md`. 5 tasks, all clean.
  - Mechanism (unchanged since, reused by every later retheme): single `theme` cookie (`light`/`dark`), no `localStorage`. `layout.tsx` (server component) reads it via `next/headers`, sets `data-theme` on `<html>` server-side — no flash of wrong theme; omitted entirely when no cookie exists, falling back to `@media (prefers-color-scheme: dark)`. `ThemeToggle` client component flips `dataset.theme` + rewrites the cookie directly (instant repaint, no reload); self-corrects its displayed position to match OS preference post-mount w/o persisting until an explicit click. Every token also aliased in Tailwind v4's `@theme inline` block (`bg-card`, `text-fg`, `border-border`, etc). `AppShell` fully re-themed this phase; every other ~30-file surface stays on ad-hoc `dark:`/system-preference (framed as deferred "Phase 2").
  - Verified live: no-cookie first load matched real OS preference w/ no flash; toggle produced instant repaint (computed colors confirmed); refresh persisted server-side; mobile drawer toggle matched. Tests (201) + `tsc` clean.
  - Side effect: the rewrite dropped a stale `body{font-family:Arial...}` rule that had been shadowing an already-existing Geist font alias — app-wide body text now correctly renders in Geist. Accepted as intended (Arial was unused scaffolding), not reverted.
- [x] Superseded (palette/font/nav): "Open Court" retheme shipped — new blue/orange token set (same names, new values, + new `--success`/`--success-bg`/`--success-fg`), swaps loaded font Geist→Barlow (body) / Barlow Condensed (headings/wordmark), restructures `AppShell` from sidebar → sticky top nav w/ hamburger mobile dropdown + a 2-tab admin sub-nav (Locations/Team). Cookie/toggle mechanism unchanged. Spec/plan `-open-court-retheme-*.md`.
  - **Correction found pre-implementation:** spec's sub-nav sketch had 3 tabs (Locations/Team/Events) but no admin-wide Events index route exists (`/admin` already serves as Locations index) — shipped w/ 2 tabs per the "no new admin functionality" constraint; a top-level `/admin/events` remains a real, undropped gap. `SuccessBanner` reclassed onto the new success tokens, no signature change.
  - The ~30-file token sweep ("Phase 2") remains exactly as deferred, now measured against Open Court instead of the earlier palette.
  - **FR found 2 Important contrast regressions + Minor doc/test gaps, all fixed:** (1) admin sub-nav active-tab text (`text-accent`, 2.80:1) failed WCAG AA — switched to `--status-fg` (4.60:1). (2) light-mode `--link` (3.68:1, regression from Phase 1's ~5.0:1) — changed to Tailwind blue-600 (5.17:1); dark-mode link untouched (~7.9:1 already). Minor: 3 scope-clarity notes added to `docs/design.md`; `scroll-padding-top:4rem` added for the new sticky header; stale README font reference updated; missing `AppShell` test case for an intentional pre-existing asymmetry.
- [x] Public landing page shipped — logged-out visitors w/ no resolved city now see a real marketing page at `/` (dark-navy hero, city-search bar, "how it works" strip, Upcoming events/Featured clubs) instead of the bare city list. Logged-in users unaffected (splits on `user`, not just city). Spec `-open-court-landing-page-design.md`. 2 new pure fns in `cityGrouping.ts` (`featuredClubs`, `filterCitiesByQuery`, unit-tested); hero search submits a plain GET to `/cities?q=`. All existing tokens except one intentional non-token hero/card literal (`#1E293B`, per spec Non-goals).
  - **FR found 2 WCAG AA failures + Minor gaps, all fixed:** (1) hero placeholder text (4.02:1) — raised to `/60` opacity (5.63:1). (2) featured-club location-count text (3.97:1 in light mode) — raised to full opacity (6.64:1); the other card variant already passed. (3) search input had no accessible label — added `aria-label`. (4) section labels were `<p>` not `<h2>` — fixed for proper landmark structure. (5) empty state always said "No locations available yet." even for a zero-match search — now shows `No cities match "{query}".` for that case (new test). (6) `searchParams.q` typed `string|undefined` but Next.js hands back `string[]` for a repeated param — would've thrown inside `.trim()` — normalized to a single string. Suite (226) + `tsc` clean.
- [x] Role-aware navigation + platform admin tier shipped — 3 additive nav tiers (player, club/location admin, new platform-wide site admin) + a click-to-open search panel (no keyboard shortcut, per explicit user direction). Spec/plan `-nav-redesign-platform-admin-*.md`. 10 tasks in an isolated worktree.
  - New `users.is_platform_admin`/`users.is_active`/`organizations.is_active` + key/value `site_settings` table (mig 0032, seeds the project owner as first platform admin). `AppShell` relabels "Admin Dashboard"→"Club admin," adds a gated "Site admin" tab, restyles sub-navs to pills. New `SearchPanel` (click-open, plain GET to `/cities?q=`, quick links to `/site-admin/*` for platform admins). 3 new `/site-admin` pages (orgs/users/settings): deactivate/reactivate any org, search/deactivate any user, grant/revoke platform-admin, edit any user's org role, set one site-wide announcement banner. Both org- and user-deactivation enforced entirely through RLS (matching every other permission check in this codebase), no app-level checks.
  - Task-review loop caught 2 real bugs: (1) new `locations select all` policy re-broke anonymous browsing for every org (the exact bug 0003 already fixed) — caught before anything built on top. (2) search-panel toggle couldn't close itself (React 18 batching race between the button's toggle and an outside-click handler) — fixed by switching the outside-click listener from `mousedown` to `click`.
  - **Manual verification surfaced a 3rd, more severe issue no task review could catch:** the pre-existing `users update own` policy (2 migrations older than this feature) has no column restriction — adding `is_platform_admin`/`is_active` in this feature's own migration created a live, exploitable privilege-escalation hole. Confirmed via a real (non-superuser) PostgREST session: a plain user could PATCH their own row to grant platform admin, then use that to deactivate an org they had no membership in — **both writes genuinely succeeded live** before being caught and reverted. Fixed immediately, same technique as the earlier `org_members.role` fix (mig 0033): `with check` pins both columns to their current stored value unless the actor is already a platform admin. Re-ran the exploit script after — both vectors rejected; confirmed an ordinary self-update (`name`) still works.
  - Also verified live: org deactivation hid it from anon `/cities`/homepage while its own `/admin` view kept showing it (reactivation restored both); user deactivation rejected a real booking-insert for a deactivated account (real-session PostgREST), allowed again after reactivation; announcement banner rendered then cleared correctly. Suite (248) + `tsc` clean throughout.
  - **FR (opus) found a 3rd instance of the exact same bug class**, this time on `organizations`: `organizations update admin` also had no column restriction, so a deactivated org's own owner/admin could self-reactivate, fully defeating moderation — confirmed live the same way, fixed in mig 0034 (same `with check` technique). Bundled w/ 3 other Important findings: (2) org deactivation didn't hide the org's *events* (`events select all` stayed `using(true)`) — narrowed in the same migration. (3) `npm run lint` had started failing (3 `no-explicit-any` in this feature's new test files, since the per-task loop never ran lint) — fixed w/ proper types. (4) a platform admin could self-lock-out of `/site-admin/*` by removing their own admin flag — hid that toggle for the signed-in admin's own row. All 4 re-verified (live for the RLS fixes). 8 Minor findings (a contrast miss, swallowed errors, unbounded user list, unescaped `ilike` wildcards, etc.) triaged as safe to leave, not fixed.
  - **One residual, deliberately left open:** `event_sessions select all` is still `using(true)`, unnarrowed alongside `events select all` — a real gap in principle (a direct anon PostgREST query could still read a deactivated org's session times/court id) but not an active leak today (no current code queries it as a base table from a public context). Worth closing next time this area is touched.
- [x] Design consistency pass shipped — finishes applying the Open Court token system app-wide: 30 files, ~257 hardcoded color occurrences, plus a booking-flow CTA color bug, event-type badge reuse, and a link-vs-button convention. No spec doc (critiqued live in chat); plan `-design-consistency-pass.md`, built via subagent-driven-development replicated manually (superpowers plugin was off; user chose to proceed without re-enabling it), 9 tasks in an isolated worktree.
  - 2 new shared primitives landed first: `EventTypeBadge`, `buttonClass` (`primary`/`secondary`/`disabled` helper). New `--error`/`--error-bg`/`--error-fg` tokens mirroring `--success` (for ~20 error banners w/ no token to move to). Flagship bug fixed: booking time-slot selection + "Continue" button rendered solid black instead of the orange accent (`TimeBlockPicker.tsx`). A parallel legacy `yellow`/`green` pending/confirmed pattern discovered mid-execution (missed by the plan's original grep) was ruled in-scope and closed w/ an added Task 9.
  - 2 per-task fix rounds: Task 5 initially restructured 2 pages' layout to fit a badge in, violating the plan's task-boundary constraint — reverted to match an existing "badge on its own line" precedent. Task 8 left 2 buttons as plain recoloring instead of `buttonClass` — fixed.
  - **FR (opus) found 3 Important cross-file findings:** (1) one page had 5 bare `border` inputs Task 5's own sweep missed. (2) the same file dropped `w-fit` on 2 register buttons when swapping to `buttonClass("primary")` (made them stretch full-width, inconsistent w/ ~19 other conversions). (3) `EventTypeBadge` was never applied on 2 pages — a seam between two tasks, neither of which owned the responsibility — and one page kept a hand-rolled duplicate of the badge's class string. All fixed + re-verified. 5 Minor findings parked (cosmetic/in-spec). Suite (257), `tsc`, `lint` all clean (1 pre-existing unrelated lint warning noted, untouched).
  - Deliberately deferred per user's own scope choice: branded 404 page, per-page `<title>`s, richer desktop layout, decorative brand touches beyond the homepage. **[All 5 closed next, see below.]**
- [x] Design polish pass shipped — closes the 5 items deferred above, done directly (no subagent loop — smaller, judgment-heavy), isolated worktree, verified live at mobile+desktop widths.
  - New themed `not-found.tsx` ("Out of bounds."). Root layout `metadata.title` became a template (`"%s | Court Scheduler"`); 27 pages set their own title (static `metadata` or `generateMetadata` for 9 dynamic pages). 5 primary pages got the wordmark's accent-period echoed after their `<h1>`. Court booking page's forecast strip got a one-line caption.
  - Richer desktop layout: widened 5 browse pages from `max-w-2xl`→`max-w-4xl`, single-column `<ul>` → `grid sm:grid-cols-2 lg:grid-cols-3` (collapses back under `sm:`); verified at 1600px and 375px. Also fixed a drift left from the previous pass: `EventTypeBadge` rendered bare/last on 2 spots on `/events` instead of on its own line — normalized.
  - Suite (257), `tsc`, `lint` all clean. One own-goal caught by lint before merge: unescaped apostrophe on the new 404 page — fixed.
- [x] Superseded: organization creation shipped, closing the `v2-org-creation-deferred` item — a real path to a 2nd org beyond hand-written SQL. Spec/plan `-org-creation-*.md`.
  - Two paths, both RLS-only (no app-level role checks): **self-serve** (`/create-club`) — any signed-in, profile-complete, not-already-a-member user creates their own club as `owner`, `is_active=false` (pending-review banner on `/admin`); **admin-assisted** (`/site-admin/orgs/new`) — platform admin creates a club + assigns an existing user as owner by email in one step, `is_active=true` immediately. New `organizations.ownership_claimed` column.
  - **The most security-eventful feature in this project's history — 5 real, live-confirmed RLS findings across 5 adversarial review rounds** before being trusted:
    1. The "zero members = brand new" guard on the `org_members` insert policy was a subquery filtered by that same table's own SELECT policy — read "zero members visible to me," not "zero members." Any signed-in user could insert themselves as owner of the real, populated Ace Volleyball Club. Fixed w/ SD helper `org_has_members` (same pattern as `is_org_member`).
    2. "Zero members" was the wrong invariant anyway — an org can lose *all* members long after creation (auth-user delete cascades; an owner can drain the roster via the pre-existing delete policy), making it indefinitely re-claimable. Fixed by tracking the real invariant: durable `ownership_claimed` flag, set once via an `after insert on org_members` trigger (immune to later member removal).
    3. That flag itself wasn't pinned — any org admin could `UPDATE` it back to `false` on their own org (reopening it to takeover) or self-escalate. The exact `is_active`-pinning bug (mig 0034) recurring on a column that didn't exist when 0034 was written. Fixed the same way.
    4. The self-serve `organizations` insert policy never required the caller be signed in at all — `anon` could insert (bounded harm, no reason to allow it). Fixed w/ an explicit `auth.uid() is not null` check.
    5. **FR (opus)** found a 5th gap: neither policy called `is_current_user_active()` (established in mig 0032 for bookings/registrations, never applied here) — a platform-admin-deactivated user could still self-serve create a pending org. Plus 2 smaller findings only visible comparing tasks side by side: the admin-assisted email lookup's `.ilike()` treated `%`/`_` as wildcards not literals (risking assignment to the wrong owner); `createOrganization` dereferenced a possibly-null `user` w/ no guard. All bundled into one fix migration (mig 0040) + one fix commit.
  - Every one of the 5 findings confirmed live against real non-superuser sessions (Admin API's `generateLink`/`verifyOtp` flow — no password typed; `DATABASE_URL`/service-role used only for read-only inspection, never as a stand-in for the access-control test itself) — several probes literally grew a real club's member count during the attack, confirmed reverted after. One verification pass produced a false "regression" traced to the probe script accidentally testing the platform-admin bootstrap account instead of an ordinary member — caught before being mistaken for a real finding.
  - All 7 spec Testing scenarios passed. Suite (258 — 3 updated for a `searchParams` prop change + 1 new case), `tsc`, `lint` all clean throughout every round. A scoped re-review of the fix wave found the email-lookup escaping was one character short (missed PostgREST's own `*` wildcard on top of SQL's `%`/`_`) and 2 spec-doc spots still described pre-fix-wave banner text — both corrected same cycle, re-verified live.
- [x] Profile photos & social links shipped — optional client-cropped avatar + optional Instagram/Facebook/X handles, set from `/profile`. Spec/plan `-profile-photos-social-links-*.md`. Implemented directly (no subagent dispatch — small scope). New `users.avatar_url`/`instagram_handle`/`facebook_handle`/`twitter_handle` + public `avatars` Storage bucket (mig 0041). `parseSocialHandle`/`buildSocialUrl` (`src/lib/socialLinks.ts`, TDD) normalize a bare handle/`@handle`/full URL down to a bare handle for storage, rebuild a canonical link (`x.com`, not `twitter.com`) for display. New hand-rolled `AvatarUploader` (no cropping library) drag/zoom-crops to a circle, draws to a 400×400 canvas, POSTs to `/api/profile/avatar` (uploads to Storage, cache-busts via `?v=<timestamp>`). New shared `Avatar` component (photo or silhouette placeholder) used on `/profile`, `/players/[userId]`, and — per explicit user decision — **unconditionally** (not gated by `share_stats_publicly`) on event Rosters, since a photo isn't the same disclosure class as a win/loss record and rosters already show names regardless. Needed a new SD RPC `get_avatar_urls(uuid[])` for that cross-user roster read. `get_public_player_stats` (mig 0024) extended w/ the 4 new columns (return-type change → drop/recreate).
  - **Real bug found only by live verification against Supabase, not by any unit test:** every upload failed w/ a generic RLS-violation error despite correct-looking insert/update/delete policies and a valid session. Root cause (found by reproducing the exact `storage-js` request directly against the Storage API, bisecting headers): an authenticated *upsert* (used here since every upload targets one fixed per-user path) internally requires a real RLS-governed `select` on `storage.objects` to detect an existing row — with no `select` policy for `authenticated`, that internal check silently denies and surfaces as a generic error on the whole request. Fixed w/ an `avatar select own` policy identical in scope to the write policies (exposes nothing an unauthenticated download of the same public object wouldn't already). A more elaborate wrong theory was chased first (`@supabase/ssr`'s server client not propagating the session JWT into `.storage` calls) and even "fixed" w/ a manual `setHeader` workaround that made no actual difference — a control test using a plain SDK client (`signInWithPassword`, no `@supabase/ssr`) failed identically, pointing at the real policy-level cause; the workaround was reverted. Full detail in the spec's "Found during manual verification" section.
  - Verified end-to-end against live Supabase w/ 2 real test accounts: uploaded/cropped a real photo, persisted across reload; entered each social handle in all 3 accepted forms, confirmed the bare handle is what's actually stored (direct DB check); visited `/players/[userId]` w/ sharing on, confirmed photo + all 3 links render; confirmed a seeded tournament roster shows the photo even viewed by a *different* account w/ `share_stats_publicly` off (matching the unconditional decision) while the name itself stays a plain non-linked label; removed the photo, confirmed both the DB column and the underlying Storage object are actually gone. Tests (268), `tsc`, `lint` all clean. Debug Storage objects removed via the Storage API afterward (raw SQL `delete` against `storage.objects` is blocked by Supabase's own `protect_delete()` trigger).
- [x] Terms of Service acceptance flow shipped (2026-09-09) — every account now affirmatively agrees to a real, versioned, timestamped document instead of the previously-unwired draft doc. Shipped w/ the draft's placeholder content **as-is** (brackets and all) — confirmed explicitly with the user first, since the app is live on Vercel; real business/jurisdiction specifics are still to come (**see `tos-details-pending-branding` memory** — fill in brackets in `docs/legal/terms-of-service-draft.md` once branding is finalized, then bump `CURRENT_TOS_VERSION` to force re-acceptance). Spec/plan `-terms-of-service-*.md`. Implemented directly, no subagent dispatch.
  - New `users.tos_accepted_version`/`tos_accepted_at` (mig 0042), checked against one code constant `CURRENT_TOS_VERSION` (`src/lib/terms.ts`, currently `"2026-09-09"`) — independent of the doc's own "Last updated" line, so a real revision is always an explicit version bump, re-prompting *every* account (all pre-existing ones start `NULL`) on next sign-in. Document stays canonical in one `.md` file — small purpose-built parser `parseMarkdownLite` (`src/lib/markdownLite.ts`, TDD against real doc fragments) turns it into blocks read server-side (`fs.readFileSync`); `TermsDocument` renders blocks to JSX w/ inline bold/code span-splitting at render time. No markdown library added (matches this codebase's hand-roll convention — `.ics`, calendar links).
  - Two gates, one recorded event (same pattern as roster-invites/user-profiles/city-personalization): a required signup checkbox (native + server-side check in `signUp`) can't itself write anything (`signUp` has no session yet) — actual write happens at the next authenticated moment, a mandatory (not skippable, unlike `/choose-city`) `/accept-terms` interstitial shown on sign-in whenever the version doesn't match. `signIn` checks this **before even the existing `next` redirect** — deliberate difference from the skippable city prompt; a legal consent gate must not be bypassable. Where sign-in lands afterward (next → org membership → city prompt → home) extracted into one shared `resolvePostAuthRedirect` (`src/lib/authRedirect.ts`), used by both `signIn` and `acceptTerms` so they can't drift. `/profile` got a "View Terms of Service" link.
  - Verified end-to-end against live Supabase: signup checkbox blocks submission until checked; fresh Admin-API test account hit the mandatory gate on first sign-in, accepting → `/choose-city` (no org/city yet); signing out/in again on that account didn't re-prompt; a real pre-existing account (`tos_accepted_version IS NULL` from before this feature) hit the gate even via a deep `?next=/bookings` link, and landed exactly back on `/bookings` after accepting, not home; the org-owner test account gated then routed to `/admin`; temporarily bumping `CURRENT_TOS_VERSION` re-triggered the gate on an already-accepted account (reverted immediately after, confirmed no account left stamped w/ the test version). Tests (280), `tsc`, `lint` all clean.

## Backlog (not scheduled)

- [ ] Text (SMS) alerts for confirmed/upcoming bookings — alongside existing email (Resend). `users.phone` exists, unused. Open questions: SMS provider (Twilio default choice), opt-in location (`/profile`, explicit not required), and reminder scheduling — this codebase has no background job runner yet, so "upcoming reminder" needs something schedule-driven (a Vercel Cron job hitting a new API route is the natural fit), not a one-off server action like the booking-confirmation email.
- [ ] "Blind Draw" random team generation — see entry above under v3 (still open).
- [ ] v4 — Payments (Stripe Connect) — not started. Manual Venmo payments (shipped, see above) is an explicit stopgap for small/private events, not a substitute.
- [ ] Minor RLS cleanup: `event_sessions select all` still `using(true)`, unnarrowed alongside `events select all` (see Role-aware navigation entry above) — not an active leak today, worth closing next time this area is touched.
- [ ] Top-level `/admin/events` admin-wide events index — doesn't exist; noted as a real gap by the Open Court retheme (admin sub-nav has no 3rd tab to link).
</content>
</invoke>
